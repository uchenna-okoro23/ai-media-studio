"use client";
import {useState} from "react";
const tools=["AI Image","AI Video","AI Frame","Storyboard"];
export default function Home(){
 const [active,setActive]=useState("AI Image"),[prompt,setPrompt]=useState(""),[history,setHistory]=useState([]),[notice,setNotice]=useState(""),[busy,setBusy]=useState(false),[showAuth,setShowAuth]=useState(false),[authMode,setAuthMode]=useState("login"),[email,setEmail]=useState(""),[password,setPassword]=useState("");
 async function generate(){if(!prompt.trim()){setNotice("Enter a prompt first.");return}setBusy(true);setNotice("");try{const res=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type:active,prompt})});const data=await res.json();setHistory(h=>[{type:active,prompt:prompt.trim(),time:new Date().toLocaleTimeString([], {hour:"2-digit",minute:"2-digit"}),status:data.status},...h].slice(0,8));setNotice(data.status==="completed"?"Generation completed.":data.error||"Provider not configured yet. Add the server-side provider key to enable live generation.");}catch(e){setNotice("Could not reach the generation service.")}finally{setBusy(false)}}
 return <main className="shell"><aside className="side"><div className="brand"><b>✦ AI Media Studio</b><small>Creator workspace</small></div><nav><button className="active">Dashboard</button><p>CREATE</p>{tools.map(t=><button className={active===t?"selected":""} onClick={()=>setActive(t)} key={t}>{t}</button>)}<p>WORKSPACE</p><button>AI Editor</button><button>History ({history.length})</button><button>Settings</button></nav><div className="usage"><span>Free usage</span><b> 3 / 10</b><div><i/></div><small>Demo limits reset daily.</small></div></aside><section className="content"><header><div><label>CREATOR WORKSPACE</label><h1>Turn ideas into media.</h1><span>Generate, organize and refine creative concepts from one workspace.</span></div><button className="upgrade" onClick={()=>setShowAuth(true)}>Sign in / Create account</button></header><div className="cards">{tools.map(t=><button className={active===t?"card activeCard":"card"} onClick={()=>setActive(t)} key={t}><strong>{t}</strong><small>{t==="AI Image"?"Create polished visuals from a prompt.":t==="AI Video"?"Turn ideas into short-form video concepts.":t==="AI Frame"?"Design keyframes and visual sequences.":"Plan scenes, shots, and narrative beats."}</small></button>)}</div><div className="workspace"><div className="panel"><label>{active}</label><h2>Describe what you want to create</h2><textarea value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Describe your idea, style, mood, camera, subject and details..."/><div className="foot"><small>{prompt.length}/2000</small><button onClick={generate} disabled={busy}>{busy?"Generating...":"✦ Generate"}</button></div>{notice&&<div className="notice">{notice}</div>}</div><div className="panel"><label>OUTPUT</label><h2>Preview</h2><div className="preview"><b>✦</b><strong>Your creation will appear here</strong><small>Connect an AI provider when you're ready for live generation.</small></div></div></div><div className="panel history"><label>RECENT</label><h2>Generation history</h2>{history.length===0?<p>No generations yet. Your recent prompts will appear here.</p>:history.map((x,i)=><div className="item" key={i}><b>{x.prompt}</b><small>{x.type} · {x.time}</small></div>)}</div><footer>AI Media Studio · Built for creators · <a href="/api/health">API health</a></footer>{showAuth&&<div className="modal"><div className="auth"><button className="close" onClick={()=>setShowAuth(false)}>×</button><label>ACCOUNT</label><h2>{authMode==="login"?"Welcome back":"Create your account"}</h2><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)"/><button className="authBtn" onClick={async()=>{const res=await fetch("/api/auth/"+authMode,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await res.json();setNotice(data.error||"Account ready.");if(res.ok)setShowAuth(false)}}>{authMode==="login"?"Sign in":"Create account"}</button><button className="switch" onClick={()=>setAuthMode(authMode==="login"?"register":"login")}>{authMode==="login"?"Need an account? Create one":"Already have an account? Sign in"}</button></div></div>}</section></main>