"use client";
import {useEffect,useState} from "react";
const tools=["AI Image","AI Video","AI Frame","Storyboard"];
export default function Home(){
 const [active,setActive]=useState("AI Image"),[prompt,setPrompt]=useState(""),[history,setHistory]=useState([]),[notice,setNotice]=useState(""),[busy,setBusy]=useState(false),[showAuth,setShowAuth]=useState(false),[authMode,setAuthMode]=useState("login"),[email,setEmail]=useState(""),[password,setPassword]=useState(""),[user,setUser]=useState(null),[usage,setUsage]=useState({today:0,dailyLimit:10});
 useEffect(()=>{refreshAccount()},[]);
 async function refreshAccount(){const me=await fetch("/api/auth/me").then(r=>r.json()).catch(()=>({}));setUser(me.user||null);if(me.user){const [a,h]=await Promise.all([fetch("/api/account").then(r=>r.json()),fetch("/api/generations").then(r=>r.json())]);if(a.usage)setUsage(a.usage);if(h.generations)setHistory(h.generations);}}
 async function generate(){
  if(!user){setShowAuth(true);setNotice("Sign in to use your free generations.");return}
  if(!prompt.trim()){setNotice("Enter a prompt first.");return}
  setBusy(true);setNotice("");
  try{const res=await fetch("/api/generate",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({type:active,prompt})});const data=await res.json();if(data.usage)setUsage(u=>({...u,today:data.usage.used}));if(res.ok){setNotice("Generation completed.");}else setNotice(data.error||"Generation failed.");if(res.ok)await refreshAccount();}catch{setNotice("Could not reach the generation service.")}finally{setBusy(false)}
 }
 async function auth(){const res=await fetch("/api/auth/"+authMode,{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email,password})});const data=await res.json();if(!res.ok){setNotice(data.error||"Account request failed.");return}setShowAuth(false);setEmail("");setPassword("");setNotice("Account ready.");await refreshAccount();}
 return <main className="shell"><aside className="side"><div className="brand"><b>✦ AI Media Studio</b><small>Creator workspace</small></div><nav><button className="active">Dashboard</button><p>CREATE</p>{tools.map(t=><button className={active===t?"selected":""} onClick={()=>setActive(t)} key={t}>{t}</button>)}<p>WORKSPACE</p><a href="/editor">AI Editor</a><a href="/settings">Settings</a></nav><div className="usage"><span>Free usage</span><b> {usage.today} / {usage.dailyLimit}</b><div><i style={{width:(usage.today/usage.dailyLimit*100)+"%"}}/></div><small>Limit resets daily.</small></div></aside><section className="content"><header><div><label>CREATOR WORKSPACE</label><h1>Turn ideas into media.</h1><span>Generate, organize and refine creative concepts from one workspace.</span></div>{user?<div className="accountChip">{user.email}</div>:<button className="upgrade" onClick={()=>setShowAuth(true)}>Sign in / Create account</button>}</header><div className="cards">{tools.map(t=><button className={active===t?"card activeCard":"card"} onClick={()=>setActive(t)} key={t}><strong>{t}</strong><small>{t==="AI Image"?"Create polished visuals from a prompt.":t==="AI Video"?"Generate short-form video with the configured video provider.":t==="AI Frame"?"Design keyframes and visual sequences.":"Plan scenes, shots, and narrative beats."}</small></button>)}</div><div className="workspace"><div className="panel"><label>{active}</label><h2>Describe what you want to create</h2><textarea maxLength={2000} value={prompt} onChange={e=>setPrompt(e.target.value)} placeholder="Describe your idea, style, mood, camera, subject and details..."/><div className="foot"><small>{prompt.length}/2000</small><button onClick={generate} disabled={busy}>{busy?"Generating...":"✦ Generate"}</button></div>{notice&&<div className="notice">{notice}</div>}</div><div className="panel"><label>OUTPUT</label><h2>Preview</h2><div className="preview"><b>✦</b><strong>{usage.today>=usage.dailyLimit?"Daily free limit reached":"Your creation will appear here"}</strong><small>Live provider output is returned by the server without exposing provider keys.</small></div></div></div><div className="panel history"><label>RECENT</label><h2>Generation history</h2>{history.length===0?<p>No generations yet. Sign in and generate to create persistent history.</p>:history.slice(0,8).map(x=><div className="item" key={x.id}><b>{x.prompt}</b><small>{x.type} · {new Date(x.created_at).toLocaleString()} · {x.status}</small></div>)}</div><footer>AI Media Studio · <a href="/api/health">API health</a></footer>{showAuth&&<div className="modal"><div className="auth"><button className="close" onClick={()=>setShowAuth(false)}>×</button><label>ACCOUNT</label><h2>{authMode==="login"?"Welcome back":"Create your account"}</h2><input type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email"/><input type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="Password (8+ characters)"/><button className="authBtn" onClick={auth}>{authMode==="login"?"Sign in":"Create account"}</button><button className="switch" onClick={()=>setAuthMode(authMode==="login"?"register":"login")}>{authMode==="login"?"Need an account? Create one":"Already have an account? Sign in"}</button></div></div>}</section></main>
}